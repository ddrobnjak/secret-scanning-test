title = "Betterleaks Config"

# 1. Inherit all built-in rules (AWS, GitHub, Stripe, OpenAI, DB strings, etc.)
[extend]
useDefault = true

# 2. DISCARD findings if they DO NOT fail token efficiency (i.e., ignore natural language / human text)
filter = '''
!filter.failsTokenEfficiency(finding["secret"])
'''
